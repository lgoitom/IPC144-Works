#define _CRT_SECURE_NO_WARNINGS 
#include <stdio.h>

/*int main(void) {
	int num1;
	int num2;
	int sum;
	printf("Enter two numbers:\n1> ");
	scanf("%d", &num1);
	printf("2> ");
	scanf("%d", &num2);
	sum = num1 + num2;
	printf("the sum is: %d\n", sum);
	return 0;
}
/*
C = A + B
B = A = C (value assigned is rightmost value)

C = A % B (modulus: the percent returns the remainder, only for int not double or float)
C = A / B (does not return remainder)

C = B && A (and: if a is true (int) and b (int) is true c is 1. otherwise if one of them is zero c will be 0)

C = A || B (or: if a is true you dont need to check b, one of them can be 0, binary operator??)

C = !A (not: if A is true, !A would be not true, opposite)




*/